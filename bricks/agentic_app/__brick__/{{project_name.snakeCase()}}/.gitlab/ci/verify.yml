# Requires a project-registered macOS runner with a shell executor and Xcode.
native_validate:
  stage: verify
  tags: [macos]
  script:
    - chmod +x tools/*.sh
    - ./tools/verify.sh
  artifacts:
    when: always
    paths:
      - {{evidence_dir}}
