# {{project_name.titleCase()}}

An agent-ready Flutter repository generated by [agentic_base](https://pub.dev/packages/agentic_base).

## Harness Contract

- Harness Contract: `v1`
- Primary profile: `{{app_profile}}` ({{app_profile_label}})
- Support tier: `{{support_tier_label}}`
- Profile summary: {{app_profile_summary}}
- Support summary: {{support_tier_summary}}
- Secondary traits: {{secondary_traits_csv}}
- Evidence directory: `{{{evidence_dir}}}`
- Required gate pack: `{{required_gate_pack}}`
- Declared Flutter toolchain: `{{flutter_sdk_manager}}` / `{{flutter_sdk_channel}}` / `{{flutter_sdk_version}}`

## Getting Started

```bash
./tools/setup.sh
./tools/run-dev.sh
```

## Canonical Context

- Machine-readable source of truth: `.info/agentic.yaml`
- Canonical docs:
  - `README.md`
  - `docs/01-architecture.md`
  - `docs/02-coding-standards.md`
  - `docs/03-state-management.md`
  - `docs/04-network-layer.md`
  - `docs/05-theming-guide.md`
  - `docs/06-testing-guide.md`
- Thin adapters:
  - `AGENTS.md`
  - `CLAUDE.md`

## Architecture

Clean Architecture with `{{state_display_name}}` state management.

```text
assets/i18n/
├── app/          # Shared app-shell translations
└── home/         # Starter feature translations

lib/
├── app/          # Bootstrap, flavors, generated i18n
├── core/         # DI, network, theme, router, error handling
├── features/     # Feature modules (3-layer)
└── shared/       # Shared widgets and utilities
```

## Deterministic Commands

```bash
./tools/setup.sh
./tools/run-dev.sh
./tools/verify.sh
./tools/build.sh dev apk
./tools/build.sh staging appbundle
./tools/release-preflight.sh prod play-production
```

Meaningful `verify` and `release-preflight` runs emit inspectable evidence under `{{{evidence_dir}}}`.

## Commands

| Command | Description |
|---------|-------------|
| `make setup` | Install deps and materialize generated code |
| `make run-dev` | Run the default dev flavor |
| `make verify` | Regenerate, analyze, test, and run native readiness checks |
| `make build` | Build one flavor and artifact type |
| `make release-preflight` | Validate credentials and tooling before upload |
| `make release` | Build and upload release artifacts without final store publish |

## State Runtime

- Source of truth: `.info/agentic.yaml` → `state_management: {{state_management}}`
- Presentation runtime: `{{state_display_name}}`
{{#uses_get_it}}- DI/runtime wiring: `get_it` + `injectable` plus `lib/app/modules/module_registrations.dart` for installed modules{{/uses_get_it}}
{{#is_riverpod}}- DI/runtime wiring: Riverpod providers plus `lib/app/modules/module_providers.dart` for installed modules{{/is_riverpod}}

## CI/CD Contract

This scaffold persists `ci_provider: {{ci_provider}}` in `.info/agentic.yaml` and emits exactly one CI provider contract:

- `github`: `.github/workflows/*.yml` calls the shared `tools/*.sh` contract
- `gitlab`: root `.gitlab-ci.yml` plus `.gitlab/ci/*.yml` call the same shared `tools/*.sh` contract

GitLab native validation is macOS-only by contract:

- register a project runner with `tags: [macos]`
- use a shell executor on a macOS machine
- install Xcode on that runner
- do not treat Linux runners as a replacement for iOS/native validation

CI preserves the same local evidence vocabulary and stores `{{{evidence_dir}}}` as an artifact.

## Release Boundary

- `./tools/release.sh` can prepare and upload Firebase, TestFlight, Play, and App Store artifacts
- credentials and signing remain human-managed
- final production store publish remains a human approval step
