name: agentic_base
description: >-
  A Dart CLI tool that generates agent-ready Flutter repositories with
  canonical context and deterministic workflows.
version: 0.1.0
homepage: https://github.com/pubs-digital/agentic_base
repository: https://github.com/pubs-digital/agentic_base
issue_tracker: https://github.com/pubs-digital/agentic_base/issues

environment:
  sdk: ^3.7.0

dependencies:
  args: ^2.6.0
  mason: ^0.1.2
  mason_logger: ^0.3.2
  path: ^1.9.1
  yaml: ^3.1.3
  yaml_edit: ^2.2.1

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.15
  very_good_analysis: ^10.2.0

executables:
  agentic_base:
